import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    alignItems: "center",
    justifyContent: "flex-start",
  },
  icon: {
    width: 44,
    height: 44,
    marginBottom: 8,
  },
  label: {
    fontSize: 14,
    textAlign: "center",
  },
});
